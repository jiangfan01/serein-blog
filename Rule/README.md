# 项目规则

这里用于存放 `Serein Blog` 的项目规则，覆盖架构、设计系统、动画、数据流等约束。

## 当前规则

- `architecture.md`：项目结构、状态边界、hooks 约定、数据流规则
- `design-system.md`：全局变量、字体、颜色、组件风格方向
- `animation.md`：动画技术栈与动效约束

后续如果有新的稳定约定，优先补充到这个目录里，不要只停留在口头说明。
